/* Bumped by hand on every change worth deploying.

   This exists because "is the new code actually live?" kept being the first
   question and the hardest one to answer from a screenshot. It shows at the
   bottom of Settings. If the number there doesn't match the number here,
   the deploy didn't take — and that's a Vercel problem, not a code one. */
export const BUILD = "2026.08.22-30";
